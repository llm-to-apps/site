# OS7 site

React + SSR + Tailwind landing scaffold.

## Commands

```bash
npm install
npm run dev
npm run build
npm run preview
```

The dev server runs the same SSR pipeline as production through `server.js`.
